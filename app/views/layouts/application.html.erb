<!DOCTYPE html>
<html>
<head>
  <title>Chatdemo</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% if flash.any? %>
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key == 'error' ? 'danger' : 'info' %>"><%= value %></div>
    <% end %>
  <% end %>
<nav class="navbar navbar-light bg-faded">
  <a class="navbar-brand" href="#">Navbar</a>
  <ul class="nav navbar-nav">
    <%= li_link_to "Home", root_url %>
    <% if user_signed_in? %>
      <%= li_link_to "Edit account", edit_user_registration_path %>
      <%= li_link_to "Sign out", destroy_user_registration_path, method: 'delete' %>
    <% else %>
      <%= li_link_to "Sign in", new_user_session_path %>
      <%= li_link_to "Sign up", new_user_registration_path %>
    <% end %>
    <% if user_signed_in? %>
      <%= li_link_to "Users", users_path %>
    <% end %>
  </ul>
  <form class="form-inline pull-xs-right">
    <input class="form-control" type="text" placeholder="Not implemented yet...">
    <button class="btn btn-success-outline" type="submit">Search</button>
  </form>
</nav>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
